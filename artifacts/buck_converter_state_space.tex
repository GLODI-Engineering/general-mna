\documentclass[11pt]{article}
\usepackage[a4paper,margin=20mm]{geometry}
\usepackage{amsmath,amssymb,booktabs,array,microtype}
\usepackage[T1]{fontenc}
\title{Buck converter: Verified State-Space Averaging}
\author{Generated by elspice-mna}
\date{1 August 2026}
\begin{document}
\maketitle

\section*{Assumptions and notation}
The converter is ideal, operates in continuous-conduction mode at fixed
switching frequency, and drives a resistive load $R$. The state vector is
$x=[i_L\;v_o]^T$, $D$ is the ON duty ratio, and $D'=1-D$. For the inverting
buck--boost, $v_o$ is the physical negative output voltage.

\section*{Independent hand derivation}
Kirchhoff's laws give
\begin{align}
ON: & L\dot i_L=v_g-v_o, & C\dot v_o=i_L-v_o/R,\\
OFF: & L\dot i_L=-v_o, & C\dot v_o=i_L-v_o/R.
\end{align}
These laws were entered independently of the MNA matrices and are used as the
verification oracle.

\section*{Switching topology models from Rust MNA}
\begin{align}
\dot x &= A_{on}x+B_{g,on}v_g,
&A_{on}&=\left[\begin{matrix}0 & - \frac{1}{L}\\\frac{1}{C} & - \frac{1}{C R}\end{matrix}\right],
&B_{g,on}&=\left[\begin{matrix}\frac{1}{L}\\0\end{matrix}\right],\\
\dot x &= A_{off}x+B_{g,off}v_g,
&A_{off}&=\left[\begin{matrix}0 & - \frac{1}{L}\\\frac{1}{C} & - \frac{1}{C R}\end{matrix}\right],
&B_{g,off}&=\left[\begin{matrix}0\\0\end{matrix}\right].
\end{align}

\section*{Input-to-output state-space representation}
State-space averaging gives
\begin{equation}
\dot{\hat x}=A\hat x+B_g\hat v_g,\qquad
\hat v_o=C_y\hat x,\qquad C_y=\begin{bmatrix}0&1\end{bmatrix},
\end{equation}
where
\begin{equation}
A=\left[\begin{matrix}0 & - \frac{1}{L}\\\frac{1}{C} & - \frac{1}{C R}\end{matrix}\right],\qquad B_g=\left[\begin{matrix}\frac{D}{L}\\0\end{matrix}\right].
\end{equation}
The corresponding transfer function is
\begin{equation}
G_{vg}(s)=\frac{\hat v_o(s)}{\hat v_g(s)}
=C_y(sI-A)^{-1}B_g=\frac{D R}{C L R s^{2} + L s + R}.
\end{equation}

\section*{Control-to-output state-space representation}
The steady operating point is
\begin{equation}
X=-A^{-1}B_gV_g=\left[\begin{matrix}\frac{D V_{g}}{R}\\D V_{g}\end{matrix}\right].
\end{equation}
Linearization with respect to duty gives
\begin{equation}
B_d=(A_{on}-A_{off})X+(B_{g,on}-B_{g,off})V_g
=\left[\begin{matrix}\frac{V_{g}}{L}\\0\end{matrix}\right].
\end{equation}
Therefore
\begin{equation}
\dot{\hat x}=A\hat x+B_d\hat d,\qquad
\hat v_o=C_y\hat x,
\end{equation}
and
\begin{equation}
G_{vd}(s)=\frac{\hat v_o(s)}{\hat d(s)}
=C_y(sI-A)^{-1}B_d=\frac{R V_{g}}{C L R s^{2} + L s + R}.
\end{equation}

\section*{Verification}
\begin{itemize}
\item \textbf{PASS} --- Rust MNA phase matrices equal the hand-derived phase equations.
\item \textbf{PASS} --- Rust numeric Schur reduction equals the SymPy descriptor reduction.
\item \textbf{PASS} --- Averaged A, input vector, DC point, and duty vector equal hand derivation.
\item \textbf{PASS} --- Input-to-output and control-to-output transfer functions simplify exactly.
\end{itemize}
All symbolic differences simplify exactly to zero. As a second check, the
Rust numeric reducer and the SymPy descriptor reducer agree at
$L=100\,\mu\mathrm{H}$, $C=220\,\mu\mathrm{F}$, and $R=12\,\Omega$.

\end{document}
