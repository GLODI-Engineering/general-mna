# Journal — 2026-08

See [`README.md`](README.md) for the rules. Newest entry first.

---

## 2026-08-28 16:00 — Close missing-docs gaps; document `build_system`/`block_graph` in README

The user asked for library documentation thorough enough that a reader (human or agent) can
use the crate from `cargo doc`/README alone, never the source. A first pass with
`RUSTFLAGS="-W missing_docs" cargo doc` found no gaps, which turned out to be a false negative
— that flag reaches `rustc`, not the `rustdoc` lint pass that actually enforces `missing_docs`.
Adding `#![warn(missing_docs)]` directly to `src/lib.rs` (the only way the lint is actually
enforced) surfaced the real picture: 90 undocumented public items, all struct/enum fields in
`block_graph.rs` (59), `transient_source.rs` (26), and `system_builder.rs` (5) — every item
itself already had a doc comment; only individual fields inside multi-field variants/structs
were missing per-field docs, which `missing_docs` still requires even when the parent variant's
doc comment already narrates every field in prose.

Added one doc comment per field across all three files (concise, cross-referencing the
existing variant-level prose rather than duplicating it), and fixed 13 broken intra-doc links
in `block_graph.rs`/`system_builder.rs` — mostly `[`name`]`-style links to types that live in
the sibling `dae-runtime`/`general-simulator` repo (this crate's `block_graph` module only
defines the block/signal-domain type vocabulary; `dae-runtime` evaluates it), which rustdoc can
never resolve as intra-doc links regardless of whether the target exists — converted to plain
`` `code` `` spans, most now annotated "(in `dae-runtime`)" for clarity. Two more were same-crate
but private-item links (`hierarchy::flatten`, `build_kind`) — same fix, since making them `pub`
just to satisfy a doc link would be a needless API-surface change.

Also updated `README.md`: neither `build_system`/`System` (the combined electrical+block-graph
entry point) nor the `block_graph` module were mentioned anywhere in it, despite being public
API — the README's examples only ever used the lower-level `MnaBuilder::build_fragment`. Added
a "Full system: electrical + block/signal-domain graph" section with a runnable example (built
and run via a throwaway `cargo run --example` first, to confirm the netlist and required PID
fields — `kp`/`ki`/`kd`/`n` and either `clamp_lo`/`clamp_hi` or `clamp_lo_in`/`clamp_hi_in` —
were actually correct before committing it to the README, not just plausible-looking).

`general-spice-core` (the sibling `spice-lsp` crate) was audited the same way and needed only
one fix: a stale `parse_line` intra-doc link that should have been `parse` (function was
renamed at some point; the doc comment wasn't updated). It already had `#![warn(missing_docs)]`
in source and genuinely zero gaps once that link was fixed — its README and crate-level doc
comment (`# Is this crate right for your use case?`, a module map, runnable examples for every
major area) were already a complete front door; nothing else needed changing there.

Verification: `cargo doc --no-deps --lib` (with and without `--features python`) clean, 0
warnings. `cargo fmt --all -- --check`, `cargo clippy --all-targets -- -D warnings`, `make
test` (Rust + Python binding tests + notebook validation, all PASS) all clean. `pre-commit
run --all-files` clean.

`general-simulator` is intentionally out of scope for this pass — the user flagged it as a
separate, more complex effort to come back to later.

## 2026-08-28 09:49 UTC — `kind=discretestatespace`/`discretetf`/`discretepid`

Three new discrete-time `BlockKind` variants, mirroring the existing `Pid`/`StateSpace`/
`TransferFunction` in shape and doc-comment style — `general-simulator`'s own math and runtime
wiring (`continuous_blocks::StateSpace::discrete_step`, the new `discrete_pid` module,
`dae-runtime`'s `BlockState::DiscreteDynamic`/`DiscretePid`) are in that repo's own journal.
Design doc lives there too (`book/dev-guide/src/discrete-time-blocks.md`), per the user's own
established "design doc first, then implement" process.

**A genuinely new parsing rule shape**: `sample_time: SampleTimeSpec` is a plain, mandatory
field on all three new variants — not `Option<SampleTimeSpec>` the way `CScript`/`PyBlock`/
`PyFunction` have it. A discrete system's own dynamics *are* its sample period; there's no
"continuous" fallback for an omitted `ts=`/`freq=` to mean, and `ts=variable` doesn't compose
with a fixed-period recursion (its coefficients are baked in at exactly one period). New helper
`parse_required_periodic_sample_time` in `system_builder.rs` wraps the existing
`parse_sample_time`, converting its `None`/`Some(Variable)` outcomes into clear, block-specific
errors instead of silently defaulting.

**Field reuse, no new grammar shapes**: `discretestatespace` accepts the exact same `a=`/`b=`/
`c=`/`d=`/`in=`/`inputs=` fields (including the MIMO matrix-or-vector-shorthand parsing) as
`kind=statespace`; `discretetf` the same `num=`/`den=` as `kind=tf`; `discretepid` the same `kp=`/
`ki=`/`kd=`/`n=`/`clamp_lo(_in)=`/`clamp_hi(_in)=` as `kind=pid`, plus one new field —
`integration_method=forward|backward|trapezoidal` (default `forward`, matching
`continuous_blocks::DiscreteIntegrationMethod`'s own three variants).

New tests in `tests/system_builder.rs` (7): matrix/sample-time parsing for
`discretestatespace`, the "missing ts=/freq=" and "ts=variable rejected" error paths,
`discretetf` parsing, `discretepid`'s default-method/fixed-clamp case, an explicit
`integration_method=` override, and an unknown-method error. All pass; `cargo build --workspace`/
`cargo test --workspace --release`/`cargo fmt --all`/`cargo clippy --workspace --all-targets --
-D warnings` clean.

---

## 2026-08-28 09:22 UTC — Logic signals: gates, an SR/fault latch, flip-flops, a counter

Design doc first (`general-simulator`'s own `book/dev-guide/src/logic-signals.md`, mirroring
`vector-signals.md`'s own per-block-kind survey style), then implementation — the user's own
explicit process for this addition. Four new `BlockKind` variants:

- `LogicGate(LogicOp)` — `kind=and`/`or`/`xor`/`nand`/`nor`/`xnor` (N-input, `N >= 2`) and
  `kind=not` (1 input). One kind, an op enum, mirroring `MathFn1`/`MathFn2`/`MathFn3`'s own
  established "one kind, many ops" shape rather than six-plus near-identical variants.
  Purely combinational, no persistent state.
- `SrLatch { priority: LatchPriority }` — `kind=srlatch`, `set=`/`reset=`, level-triggered, no
  clock. `priority` resolves the `set && reset` tie (default `Set`, for the motivating fault-
  latch use case: a fault occurring the same step as a reset command should still latch).
- `FlipFlop { kind: FlipFlopKind, reset: bool }` — `kind=dff`/`jkff`/`tff`, edge-triggered
  (rising `clk` only), optional synchronous `reset=`.
- `Counter { up_down: bool, modulus: Option<u32>, reset: bool }` — `kind=counter`, same
  rising-edge skeleton as `FlipFlop`, generalized from a bit to a signed running count.

**Revises, not overrides by oversight, a documented prior decision**: `continuous-blocks`'s own
`waveform_arithmetic.rs` module doc explicitly excludes boolean/comparison operators as
simulation blocks ("belong to a plot-expression language for post-processing... not simulation
blocks"). That reasoning holds for *stateless* combinational logic (an `if`/threshold chain can
already express an AND/OR of two signals) — it doesn't hold for *sequential* logic with genuine
memory (a fault latch, a flip-flop, a counter), which no combination of existing stateless
expression blocks can hold across steps at all. Recorded explicitly in the design doc rather
than silently contradicted.

New shared `LogicOp`/`LatchPriority`/`FlipFlopKind` types plus `as_bool`/`from_bool`/
`rising_edge`/`srlatch_next` helpers live in a new `continuous-blocks` module,
`crates/continuous-blocks/src/logic.rs` (11 unit tests, hand-derived truth tables including the
N-input XOR odd-parity generalization past 2 inputs, which is easy to get wrong). New
`system_builder.rs` parsing for all four kinds, plus 6 parse-level tests. The
`general-simulator`-side evaluation wiring (`dae-runtime`'s `BlockState`, edge detection across
real transient steps, the interlock/fault-latch end-to-end CLI demonstration) lives in that
sibling repo's own journal.

Verification: `cargo build`, `cargo test` (13/13 `system_builder.rs` tests, 0 failures),
`cargo fmt --all -- --check`/`cargo clippy --all-targets -- -D warnings` both clean.

## 2026-08-28 08:51 UTC — `SampleTimeSpec`: block-controlled and phase-offset sample times

Two related additions to `sample_time` for `kind=cscript`/`kind=pyblock`/`kind=pyfunc`, both
requested after reading another block-diagram tool's own manual for its own code-block feature
(never named — see the standing generic-description convention this project follows for such
tools). `sample_time` on
`BlockKind::CScript`/`PyBlock`/`PyFunction` changed from `Option<f64>` to the new
`Option<SampleTimeSpec>` (`Periodic { period, offset }` or `Variable`), a real breaking type
change to those three struct fields — every caller across `general-mna`/`general-simulator`
updated.

**Phase offset** (`to=`): `ts=`/`freq=` alone still fix a period, but now `to=X` can delay when
the *first* hit lands to `t=X` instead of the period's own implicit anchor. **Real correction
made during design, not before**: the plan (and what got told to the user beforehand) was that
the pre-existing default behavior was "first hit one full period after `t=0`," so `offset`
should default to `period` to preserve it. Reading `general-simulator`'s own `dae-runtime`
construction code directly (not assumed) showed this was wrong — the actual pre-existing
behavior is "first hit immediately, on the very first `evaluate_blocks` call, regardless of
`period`'s value" (the accumulator is seeded to `period` specifically so it's already past
threshold on the first `+= dt`). Fixed the design before implementing: default `offset = 0.0`
(when `to=` is omitted) is what actually preserves this — an explicit `to=X` (validated
`0.0 <= X < period`) is what newly delays the first hit. Every pre-existing `ts=`/`freq=`-only
test in the whole workspace still passes unchanged, confirming this correction, not the original
plan, was right.

**Variable (block-controlled) sample time** (`ts=variable`): the block itself decides its own
next execution time at runtime instead of a fixed period — for a block whose own next event is
known only analytically at runtime. Rejected at parse time for `kind=pyfunc` specifically (a
stateless function call has no instance to remember a requested next-hit time against); only
`cscript`/`pyblock` support it. The `general-simulator`-side runtime implementation
(`cscript_next_sample_hit`/Python `next_sample_hit`, the `dae-runtime` due-gating changes) lives
in that sibling repo's own journal.

New shared helper `parse_sample_time` in `system_builder.rs` replaces three near-identical
inline `match` blocks (one per `"cscript"`/`"pyblock"`/`"pyfunc"` arm) that used to duplicate the
same `ts=`/`freq=` mutual-exclusivity logic — also fixes a bug introduced while writing it: an
`Edit` landed the new function's own doc comment between `parse_const_value`'s existing doc
comment and its function, silently orphaning that doc comment. Caught and fixed before
committing, not after.

Verification: `cargo build`, `cargo test` (all 5 test binaries, 0 failures, including every
pre-existing `sample_time`-using test unchanged), `cargo fmt --all -- --check`/`cargo clippy
--all-targets -- -D warnings` both clean.

## 2026-08-27 15:50 UTC — Remove `BlockKind::Sig2Gate`, merge into `Sig2Voltage`

User: "remove the sig2gate and let sig2v do the job because our mosfet is voltage controlled
and there is no need for dedicated controller." A MOSFET's gate is itself a voltage (`V_GS`
against `v_th`), not a distinct discrete-actuation signal domain, so the separate `Sig2Gate`
converter this project added earlier (2026-08-21, "Enforced physical/signal-domain converters")
was unnecessary weight: removed the variant, the `"sig2gate"` netlist keyword, and its
`block_kind_name` entry. `GateBinding::Block`'s target requirement (enforced in
`general-simulator`'s `dae-runtime`, not here) now names `Sig2Voltage` instead — the same
converter a `V`-source's own literal-value field already used, so one type now covers both "a
signal-domain block drives a source's own magnitude" and "a signal-domain block drives a
MOSFET's gate." `BlockKind::Sig2Voltage`'s own doc comment rewritten to describe both roles;
`GateBinding`'s doc comment and `resolve`'s panic message updated to reference `Sig2Voltage`
instead of the now-removed `Sig2Gate`. The `general-simulator`-side enforcement rename
(`DaeError::GateTargetNotSig2Gate` → `GateTargetNotSig2Voltage`) and every fixture/test update
live in that sibling repo's own journal.

Verification: `cargo build`, `cargo test` (all 5 test binaries, 0 failures) both clean.

## 2026-08-27 17:23 UTC — `BlockKind::PyFunction`: the `"pyfunc"` parsing arm (the `general-simulator`-side implementation lives there)

Added the type/parsing half of `kind=pyfunc` — a new, separate variant alongside `BlockKind::
PyBlock` above, not a mode of it, for a plain, stateless Python function called positionally
(`f(*inputs)`, no `start`/`state`/`t`/`dt`, no `xc_count`). `system_builder.rs`'s new `"pyfunc"`
arm mirrors `"pyblock"`'s own `outputs=`/`inputs=`/`ts=`/`freq=` parsing but requires `function=`
(no default `output` name, since the whole point is calling a descriptively-named function) and
has no `xc_count=` field at all. Full design and the `general-simulator`-side implementation
(`pyblock-ffi`'s new `pure_function` submodule, `dae-runtime`, `general-simulator-cli`) live in
`general-simulator`'s own journal and `book/dev-guide/src/pyfunc-blocks.md`. All existing
`general-mna` tests unaffected (confirmed via a clean `cargo build`/`cargo test`, all 5 test
binaries `ok`).

---

## 2026-08-27 14:57 — `BlockKind::PyBlock`: Python function blocks (the `general-simulator`-side implementation lives there)

Added the type/parsing half of `kind=pyblock` — a scripting-language-code-block-style escape
hatch to Python, the direct sibling of `BlockKind::CScript`. `path` names a `.py` file instead of a
precompiled `.so`; `output_names`/`sample_time`/`xc_count` all mean exactly what they mean for
`CScript` (deliberately scalar-only outputs, the same zero-order-hold `sample_time` convention,
the same solver-integrated-vs-hand-managed continuous-state split). `system_builder.rs`'s own
`"pyblock"` parsing arm mirrors `"cscript"`'s field set exactly (`path=` instead of `lib=`,
otherwise identical `outputs=`/`inputs=`/`ts=`/`freq=`/`xc_count=` parsing).

The actual Python-embedding machinery (a new `pyblock-ffi` crate, PyO3's `auto-initialize`
feature) and the `dae-runtime` evaluation arm live in `general-simulator`'s own repo — see that
repo's own journal entry, same session, for the full design and the real measured performance
numbers behind it (`book/dev-guide/src/python-blocks.md`).

`cargo fmt`/`clippy --all-targets -D warnings`/`test --release` all clean; no existing test
needed updating.

---

## 2026-08-27 12:44 — `SignalValue`: vector-signal support (per the design survey), decisions applied

Implements `book/dev-guide/src/vector-signals.md`'s design (`general-simulator` repo) with the
three open questions it flagged now answered: `Gain`/`Const`'s matrix/vector variant is a richer
payload on the *existing* kind, not a new sibling `BlockKind` (`Gain(GainValue)`,
`Const(ConstValue)`, each `Scalar | Matrix`/`Scalar | Vector`); the fixed-arity multi-output
blocks (`CoordinateTransform`/`Pmsm`/`cscript`) keep scalar-only outputs, no additive
`vector_output=` alias added.

New `SignalValue { Scalar(f64), Vector(Vec<f64>) }` — the value type flowing through the graph
from now on, replacing bare `f64` in every `outputs`/`Signal::resolve` path (evaluation side
lives in `dae-runtime`, same as always; this repo only owns the type and its own two blocks'
parsing). `GateBinding::resolve` now requires `Scalar`, panicking (documented unreachable, same
"checked once up front" discipline as everything else in this crate) if it ever sees a `Vector`
— can't happen validly, since the only legal gate target (`Sig2Gate`) itself rejects a `Vector`
input at evaluation time.

`system_builder.rs`: `kind=const value=` accepts a bare scalar (unchanged) or a flat Python list
(a constant vector); `kind=gain k=` accepts a bare scalar (unchanged) or a *nested* Python list
(a matrix, for a real matrix-vector product) — a flat list is rejected there specifically, since
`Gain`'s own value is never itself a vector, unlike `Const`'s. `kind=statespace` gained real MIMO
support: `b=`/`c=` accept either their original flat-list SISO shorthand or a genuine matrix,
`d=` defaults to an all-zero matrix of the right shape when omitted (generalizing the old `0.0`
default) and only accepts a bare scalar when the system is genuinely 1x1, and `inputs=` (new,
alongside the existing `in=`) takes a comma list for a multi-input system.

Every existing test (general-mna's own, plus the full `general-simulator` workspace) needed only
mechanical `ConstValue::Scalar(...)`/`GainValue::Scalar(...)` wrapping at direct-construction
call sites — zero behavioral changes for any existing netlist or hand-built `BlockInstance`.
`cargo fmt`/`clippy --all-targets -D warnings`/`test --release` all clean.

---

## 2026-08-27 11:54 — `BlockKind::CScript` gains `xc_count`, the optional continuous-state field

Requested after a design discussion about `cscript`'s state model, framed around the general
distinction a dynamically-loaded-block formalism can draw between a continuous state (`xc`,
numerically integrated by the solver) and a discrete one (`xd`, updated by the block's own code
at its own rate). `general-simulator`'s `cscript` already had the `xd` half in full (the opaque
`void *state` blob, mutated inside `cscript_output`) but nothing for `xc` — a state the *solver*
integrates, not the block's own hand-rolled Euler step.

Added `xc_count: usize` to `BlockKind::CScript` here (default `0`, fully backward compatible —
every existing `cscript` block in the sibling `internal-archive` repo is untouched) and
`xc_count=<n>` parsing in
`system_builder.rs`'s `"cscript"` match arm. `xc_count > 0` switches a block from the plain
single-function contract (`cscript_output`) to a new one (`cscript_derivative`/
`cscript_output_xc`, exported *instead of* `cscript_output`) — see `general-simulator`'s own
`cscript-ffi`/`dae-runtime` changes (same session) for the actual RK4 integration and FFI
plumbing; this repo only owns the type/parsing half, per the Phase-2 format-unification split
(`block_graph.rs`'s own module doc comment).

`cargo fmt`/`clippy --all-targets -D warnings`/`test --release` all clean; no existing test
needed updating (the field defaults to `0` and no test here declares `kind=cscript` directly —
end-to-end coverage for both `xc_count=0` and `xc_count>0` lives in `general-simulator-cli`'s
own `tests/cscript.rs`).

---

## 2026-08-25 14:03 — Fix: `kind=probe`'s `node=`/`branch=` fields weren't hierarchy-mangled

Found while summarizing remaining Phase 3 work, before any live test caught it: `hierarchy.rs`'s
`mangle_block` only dotted-path-mangled a block's *signal* reference fields (`in`/`inputs`/
`ctrl`/`clamp_lo_in`/`clamp_hi_in`). A `kind=probe` block's `node=`/`branch=` fields — which name
an *electrical* node/element, not a signal (see `system_builder::build_kind`'s `"probe"` arm) —
were never touched, so a probe declared inside a `.subckt` body pointing at a purely-internal
node (not a declared port) silently kept its unmangled name after flattening: either an unresolved
reference downstream, or worse, an accidental match against an unrelated same-named node
elsewhere in the flattened circuit.

Fixed by adding a second field set (`ELECTRICAL_REF_FIELDS = ["node", "branch"]`) resolved via
the exact same `resolve` closure `expand_body` uses for `ElementInstance` nodes (ground-aware,
port-aware) — each is a single value, never comma-separated or `prev:`-prefixed (that's a
signal-domain-only convention). Added a regression test
(`a_probe_inside_a_subckt_body_resolves_its_node_field_to_the_dotted_path_name`) and tightened an
existing one to actually assert the probe's `node` field value (it previously only checked the
block's own name, silently missing this exact gap). Verified end-to-end through
`general-simulator`'s CLI: a probe on a purely-internal node inside a subckt appears as its own
correctly-valued dotted-path CSV column (`V(X1.mid)`), matching hand-calculated voltage-divider
math. `cargo fmt`/`clippy -D warnings`/`test --workspace` all clean.

---

## 2026-08-24 14:39 — External signal binding into a subckt's block graph (Phase 3 follow-up)

The previous Phase 3 entry deferred binding a signal from *outside* a `.subckt` instance into
its own internal block graph, on the assumption it would need new port-domain-inference logic
and an extended `X`-line syntax. Turned out unnecessary: `hierarchy::flatten`'s `port_map`
substitution is already domain-agnostic — it resolves a name via the caller's binding or falls
back to dotted-path prefixing, with no idea whether that name is an electrical node or a signal.
Since `mangle_block` already runs the *same* `resolve` closure `expand_body` uses for electrical
element nodes, declaring an extra "port" purely to carry a signal binding — no electrical meaning
at all — already worked, with zero code changes needed.

Two directions, both confirmed by test and by an end-to-end CLI smoke run:

- **Signal in**: `.subckt reg vin vout ctrl` with an internal block field `in=ctrl`; calling
  `X1 vsrc vload EXT_DUTY reg` binds `ctrl` to `EXT_DUTY`.
- **Signal out**: an internal block whose own `.name` *is* the declared port name becomes
  addressable from outside under whatever the caller bound that port to — symmetric with how an
  internal node named the same as an electrical port already works.

Rewrote `hierarchy.rs`'s module doc comment (previously claimed this was unsupported) to document
both directions with worked examples. Added two new unit tests
(`a_signal_can_be_bound_into_a_subckt_instance_from_outside_via_a_declared_port`,
`a_subckt_instance_can_expose_an_internal_block_as_a_signal_output_port`) — 27 tests total now,
all passing. Verified end-to-end through `general-simulator`'s real CLI binary: a MOSFET declared
inside a `.subckt` body, gated via a declared `ctrl` port bound to an externally-declared
`kind=const` → `kind=sig2gate` chain, correctly switches on (`vout ≈ 5·1000/1000.1V` for a
0.1Ω on-resistance switch, matching hand calculation).

`cargo fmt`/`clippy -D warnings`/`test --workspace` all clean.

---

## 2026-08-24 14:32 — Phase 3: `.subckt`/`X`-instance hierarchical flattening

Added `hierarchy::flatten` (new module `src/hierarchy.rs`): expands every `.subckt`/`X`-instance
pair in a parsed `Statement` list into one flat list, dotted-path-prefixing every internal
device/block name by its instantiation chain (`X1.R1`, `X1.X2.C3`, ...) so repeated instances of
the same subcircuit never collide. Handles nested subckt instantiation (dotted paths compose),
positional port binding (a `.subckt`'s declared node list ↔ the `X`-call's own node list),
recursive-instantiation detection (a cycle errors instead of infinite-looping), and node-count
mismatches — all with clear `Result<_, String>` errors. Block/signal-domain statements
(`Statement::BlockInstance`) declared inside a `.subckt` body are flattened the same way: the
block's own name and every signal-reference field (`in`/`inputs`/`ctrl`/`clamp_lo_in`/
`clamp_hi_in`, `prev:`-prefixed values handled) get the same dotted-path resolution; `outputs=`
entries are always instance-prefixed (new names, never references).

**Scope explicitly deferred, not an oversight**: binding a signal from *outside* a subckt
instance into its internal block graph (a genuine signal-domain port) isn't implemented this
pass — every block reference inside a body must resolve within that body or through an
electrical port. This was surfaced to and confirmed acceptable by the user before implementation;
a real narrowing relative to the original Phase 3 plan text, which mentioned this as in-scope.

**Real bug caught by end-to-end smoke-testing, not by the unit tests**: ground (`"0"`/`"gnd"`,
case-insensitive) was initially being dotted-path-mangled like any other node inside a subckt
body, turning a capacitor's ground terminal into a private, per-instance node instead of the one
true globally-shared reference — silently open-circuiting it (a capacitor whose only other
terminal is an untouched private node carries zero current by KCL). Caught by building a
two-stage RC-lowpass-via-subckt netlist and running it through the real CLI: the output showed no
actual RC time-constant behavior at all (just the source's own built-in ramp shape passing
through what was effectively a bare resistor divider) even though every unit test passed. Fixed
by special-casing ground in `hierarchy::flatten`'s node-resolution closure to never be mangled or
port-substituted; added a dedicated regression test
(`ground_inside_a_subckt_body_stays_the_global_reference_not_a_private_node`) exercising exactly
this two-instance-RC-leg shape. Re-verified end-to-end afterward: the same netlist now shows a
correct ~1ms (`R*C = 1k*1uF`) charge/discharge curve per stage.

New public API: `general_mna::parse_and_flatten(source, dialect) -> Result<Vec<Statement>,
String>` (parse + flatten, the shared first step `build_system` itself now uses) — exposed
specifically so `general-simulator`'s `dae-runtime` crate can flatten once and reuse the same
statement list for its own from-scratch `MnaSystem` rebuilds (per-timestep switch-state
rebuilding needs fresh `MnaBuilder::build_statements` calls, not a single cached `MnaSystem`) —
see `general-simulator`'s own journal entry for that side of the integration, which was the
larger part of this work (`dae-runtime` previously re-parsed raw netlist text independently at
~10 call sites, completely bypassing hierarchy).

8 new tests in `hierarchy.rs` (7 `flatten` behavior tests + the ground regression), 1
`system_builder.rs` test updated to instantiate its subckt explicitly (uninstantiated subckt
bodies correctly contribute nothing now, a real, intended behavior change from Phase 2's
"leaks to top level unconditionally" — Phase 2's own test asserted the pre-hierarchy behavior).
`cargo fmt`/`clippy -D warnings`/`test --workspace` all clean.

---

## 2026-08-20 11:20 — Fix: `TransientFunction::parse` rejected SPICE-suffixed arguments

Follow-up to the entry directly below. While smoke-testing the new time-varying sources
end-to-end through `elspice-pwl`'s real CLI (not just this crate's own unit tests, which only
ever used plain numeric literals), `PULSE(0 5 1m 100n 100n 2m 4m)` — an entirely ordinary
netlist line — failed to build at all: `unsupported scalar expression 'PULSE(0'; use a number
or symbol`. Root cause: `TransientFunction::parse`'s numeric-argument parsing used raw
`str::parse::<f64>()`, which doesn't understand SPICE suffixes (`1m`, `100n`, ...); it failed
silently via `?`, so `parse()` returned `None` and the whole source fell through to the old,
now-inapplicable scalar-only path, which then correctly rejected the leftover `PULSE(0` token
as garbage. Confirmed via a scratch probe that `spice-core`'s own tokenization was not at
fault. Fixed by making `expression::parse_spice_number` `pub(crate)` and calling it directly
from `transient_source.rs`, instead of `f64::parse`. Added two new unit tests exercising
suffixed arguments (`PULSE(0 5 1m 100n 100n 2m 4m)`, `SIN(0 10 50k 0 0 0)`) — the exact gap
that let this through the first time. `cargo fmt`/`clippy -D warnings`/`test`/`build
--all-features` all clean. Re-verified end-to-end through `elspice-pwl-cli` for all five
source kinds afterward (tracked in `elspice-pwl`'s own journal).

---

## 2026-08-20 07:49 — Time-varying `V`/`I` sources: `SIN`/`PULSE`/`EXP`/`PWL`/`SFFM`

Requested directly by the user for `elspice-pwl` (the TIDA-010954 cycloconverter experiment
needed a genuinely time-varying `Vac(t)`), pointing at `spice-core`'s own grammar reference
(`spice-lsp/docs/GRAMMAR.md`) for the exact forms: the same five transient-function names
common to both ngspice and Xyce.

Confirmed directly (not assumed) before writing anything: `spice-core` already tokenizes a
`SIN(0 10 1000)`-style source losslessly — `["SIN(0", "10", "1000)"]`, parens stuck to
whichever token they landed on, but every number recoverable — so no `spice-core` change was
needed at all, only interpretation on this crate's side. Also confirmed `elspice-mna` already
has exactly the mechanism needed: a `V`/`I` source's `B` column is stamped once, generically,
by `stamp_voltage_source`/`stamp_current_source` regardless of what the source's own literal
value is — the *value* only enters via `input_values[column]`, a symbolic `Expression`
evaluated fresh on every `MnaSystem::evaluate` call. A PWL diode's own Norton current
(`{name}_Ioff`) already uses this as a per-step-overridable *symbol* rather than a baked
literal; a `V`/`I` source with a transient function just needed the same treatment, which no
prior netlist ever needed since every source so far was a plain constant.

New `transient_source` module: `TransientFunction` (`Sin`/`Pulse`/`Exp`/`Pwl`/`Sffm`, pure data
plus a pure `value_at(t) -> f64`, hand-verified against textbook values for each form — a
`PWL`'s own breakpoint list is heap-allocated, not a fixed-size inline array, after `clippy`
flagged the enum ballooning to 528 bytes for a sized-for-worst-case `PWL` even on a plain
`SIN`) and `TransientFunction::parse` (detects and reassembles a call from `spice-core`'s own
mangled tokenization). `builder.rs`'s per-source loop now tries `TransientFunction::parse`
first; if it matches, the source becomes `Expression::symbol(name)` (the diode pattern) and its
parsed function is recorded in a new `MnaSystem::transient_sources: BTreeMap<String,
TransientFunction>` field for the caller to read; if it doesn't match, exact existing behavior
(a baked literal from `source_value`) is unchanged — confirmed via a new test
(`plain_dc_source_is_unaffected_by_transient_source_support`) and the full existing suite
passing unmodified.

**This crate has no notion of "now"** — it hands the caller a `TransientFunction` and the
caller is responsible for calling `value_at(t)` and passing the result into `evaluate`'s
`values` map at every step, exactly the same delegation already used for diode Norton currents.
Wiring that into `elspice-pwl`'s own transient loop (`dae-runtime`, which currently has no
notion of "the current absolute simulated time" threaded into its `fold_and_solve` calls at
all) is a separate, not-yet-done change — tracked in `elspice-pwl`'s own journal.

`cargo fmt`, `cargo clippy --all-targets -- -D warnings`, `cargo test`, and
`cargo build --all-features` (the optional `python` binding) all clean.


## 2026-08-17 10:01 — Add 'D' (diode) stamp + numeric RHS evaluation, for `elspice-pwl`

A new sibling repo, `elspice-pwl`, is building a Newton-free piecewise-linear circuit
simulator (LCP-based mode selection instead of Newton-Raphson + voltage limiting) and needs
this crate's linear-device stamping for the non-PWL parts of a circuit. Its own diode/MOSFET
companion models are resistor-plus-current-source (`I = G*V + I_off`) between two *existing*
nodes — no new MNA unknowns needed — so the minimal, additive extension was: recognize `'D'`
as a stampable device (`src/builder.rs`), reusing the existing `stamp_admittance` +
`stamp_current_source` functions verbatim with two new per-instance symbolic parameters
(`{name}_G`, `{name}_Ioff`) instead of parsed netlist values, mirroring the `Ron`/`Roff`
pattern switches already use.

This exposed a real gap: `NumericMnaSystem::evaluate()` only evaluated `a`/`k`/`b`, never the
RHS (`u`/`input_values`) — fine while every source's value was fixed at parse time, but a
diode's `Ioff` genuinely changes every timestep. Added `input_values: Vec<f64>` and
`u: Vec<f64>` to `NumericMnaSystem`, computed the same way the symbolic `u` already is.

First attempt made input-value evaluation mandatory (error on a missing symbol), which broke
`converter_switch_phases_average_with_duty_ratio` — that test deliberately leaves `Vin`
unresolved while only checking the averaged `a`/`k` matrices, a legitimate pre-existing
pattern. Fixed by making it best-effort (`f64::NAN` for an unresolved input, contributing
`0.0` to `u`) rather than failing the whole evaluation — matches how `a`/`k`/`b` evaluation
already only requires the symbols actually needed for what's being computed.

Also updated `nonlinear_devices_are_not_silently_discarded` (its 'D' fixture is now
intentionally supported; retargeted at 'Q', which still correctly errors) and added
`diode_is_stamped_as_symbolic_conductance_plus_norton_current_source`, hand-tracing the
expected `a`/`b`/`u` expressions from the stamping functions' own source before writing the
assertions, not just checking the code runs.

Verification: `cargo fmt --all -- --check`, `cargo clippy --all-targets --features python -- -D
warnings`, `cargo test --all-targets` and `--all-targets --features python` all clean (15
tests total, all passing). Did **not** run the full `make test` gate this session — that also
builds the PyO3/maturin binding and runs the Python unit tests + notebook validation, none of
which touch the changed surface (confirmed no `NumericMnaSystem` usage in `src/python.rs`;
only the unrelated symbolic `StringMnaSystem.u`/`.input_values` fields are exposed there) —
flagging this explicitly rather than silently skipping it, per this repo's own verification
discipline. Worth running before the next Python-facing change lands.

---

## 2026-08-01 19:21 — Adopt reusable repository workflow

The user asked to bring useful hooks, skills, and dated-journal practices from
`general-simulator` and `spice-lsp` without importing their project-specific
epic orchestration. Added tailored agent guidance, conventional-commit and file
hygiene hooks, commit/model-verification/gotcha skills, an indexed gotcha store,
and this monthly journal. The MNA verification skill preserves the useful
analytic-reference discipline from the simulator's circuit-testing workflow
while omitting `.gsim`, external-solver, IPC, and waveform-storage procedures
that do not belong to this library.

Verification for this changeset: skill schema validation, hook execution, and
the full `make test` gate. Remaining work is tracked in the repository rather
than in a progress-epic file; this small library does not need the source
projects' coder/tester arbitration system.
